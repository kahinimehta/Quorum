"""cua — Conclusion Update Agent engine package.

The task-agnostic engine (this package, minus `nih/`) plus the domain binding under `nih/`.
Engine modules name nothing domain-specific (CONVENTIONS rule 1); every domain noun lives only under
`cua.nih`. Owner: builder 1 (S2 skeleton; S4/S5 roles).
"""
