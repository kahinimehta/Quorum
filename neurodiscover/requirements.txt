# NeuroDiscover runtime dependencies
#
# Python: 3.10, 3.11, or 3.12 (required; 3.13+ not yet tested with biomcp-python)
# sqlite3 is included in the Python standard library.
#
# Install (pip / venv):
#   pip install -r requirements.txt
#
# Conda:  see environment.yml
#
# Install (conda — empty environment):
#   conda env create -f environment.yml
#   conda activate neurodiscover
#
# Start dashboard:
#   macOS/Linux:  make dashboard
#   Windows:      python cli.py dashboard
#
# Optional dev / docs screenshots:
#   pip install -r requirements-dev.txt

biomcp-python>=0.7.3,<0.8
openai>=1.40.0
python-dotenv>=1.0.0
psycopg[binary]>=3.1.18
requests>=2.31.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
